package calc

//method to calculate the height and weight and BMI

func calculate(height string, weight string) int {
	var bmiValue int

	heightVal := int(height)
	weightVal := int(weight)

	//formula to return the calculation
	return bmiValue
}
